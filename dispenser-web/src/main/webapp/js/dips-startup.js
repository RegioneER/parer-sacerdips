/*
 * Funzione "main" che effettua la chiamata alla dips_onLoad() standard o quella ridefinita nelle ricerche specifiche.
 * E' l'ultimo script che viene richiamato dalle varie jsp standard di ricerca di dips.
 */
$(document).ready(function () {

    dips_onLoad();

});
