function dips_onLoad() {

    /* Inserire in questo metodo il codice di inizializzazione particolare della ricerca configurata
     * che verrà richiamato dal "framework" di dispenser durante la document.ready di jquery
     * nel momento in cui la pagina viene caricata.
     * Prendere ad esempio le ricerche già configurate per Pievesestina e LUM
     */

}
